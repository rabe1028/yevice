# Tools installed via mise (https://mise.jdx.dev/).
#
# Setup:
#   mise install        # install all tools listed below
#   mise exec -- <cmd>  # run with mise PATH
#
# When direnv + mise activate hook is configured, `cd` into this directory
# is enough — the tools become available on PATH automatically.

[tools]
# Local GitHub Actions runner used by `scripts/verify-ci.sh` to validate
# .github/workflows/ci.yml before pushing.
#
# Pinned to 0.17.0 because 0.18.0–0.22.1 ship a JS syntax error in the
# compiled dist file ("Missing } in template expression") that aborts on
# startup. Upstream issue: revisit when a newer npm release is available.
"npm:@mizchi/actrun" = "0.17.0"

# Cargo subcommands used by CI (matches jobs in .github/workflows/ci.yml).
# Rust toolchain itself is managed via rust-toolchain.toml (channel = stable).
"cargo:cargo-deny" = "0.19.8"     # license / advisory / bans / sources lint
"cargo:cargo-machete" = "0.9.2"   # unused-dependency detection
"cargo:cargo-llvm-cov" = "0.8.7"  # coverage (lcov.info) via llvm source-based
"cargo:cargo-nextest" = "0.9.137" # faster, parallel test runner
"cargo:cargo-mutants" = "27.0.0"  # mutation testing (run on demand)
"cargo:cargo-cyclonedx" = "0.5.9" # SBOM (CycloneDX) generation
"cargo:taplo-cli" = "0.10.0"      # TOML formatter / linter
"cargo:typos-cli" = "1.19.0"      # source-code spell checker
"cargo:prek" = "0.4.3"            # Rust-based pre-commit hook manager
